{
  "name": "@nexum-network/api",
  "version": "0.1.0",
  "type": "module",
  "description": "Minimal Nexum HTTP API for challenge generation and response verification.",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test \"test/**/*.test.js\""
  }
}
