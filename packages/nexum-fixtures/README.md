# @nexum-network/fixtures

Reference JSON fixtures for Nexum protocol tests and demos.

Fixtures should be:

- synthetic,
- free of private key material,
- stable enough for protocol compatibility tests,
- small enough to inspect in code review.
